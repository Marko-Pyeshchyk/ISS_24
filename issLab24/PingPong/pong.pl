%====================================================================================
% pong description   
%====================================================================================
request( hit, hit(N) ).
reply( hit_resp, hit(N) ).  %%for hit
%====================================================================================
context(ctx_pong, "localhost",  "TCP", "8069").
 qactor( pong, ctx_pong, "it.unibo.pong.Pong").
 static(pong).
